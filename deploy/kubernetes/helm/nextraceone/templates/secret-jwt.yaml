apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nextraceone.fullname" . }}-jwt-secret
  labels:
    {{- include "nextraceone.labels" . | nindent 4 }}
type: Opaque
data:
  secret: {{ .Values.app.jwt.secret | default (randAlphaNum 64) | b64enc | quote }}
