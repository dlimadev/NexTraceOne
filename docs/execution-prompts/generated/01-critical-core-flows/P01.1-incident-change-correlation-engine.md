# PROMPT — Implement dynamic incident↔change correlation engine

## 1. Identification
- **Prompt ID:** P01.1
- **Phase:** 01-critical-core-flows
- **Execution order:** 4
- **Type:** feature-implement
- **Area:** backend
- **Module:** operationalintelligence
- **Bounded context:** incidents-correlation
- **Can run in parallel:** yes (with P01.4, P01.6)
- **Risk level:** high

## 2. Objective
Implement a dynamic correlation engine that links incidents to changes based on temporal proximity, affected services, and environments. Create two new endpoints: one to trigger correlation, one to retrieve correlated changes. Persist correlation results.

## 3. Problem actual
The forensic audit confirms: "Incident↔change correlation is 0% functional. Infrastructure for persistence exists (EfIncidentStore, 678 lines, IncidentDbContext with 5 DbSets). Missing: dynamic correlation engine that links incidents to changes via timestamps and services." Currently, the only correlation data is static seed JSON, not dynamically computed.

## 4. Resultado esperado
- A `CorrelateIncidentWithChanges` CQRS handler that queries both IncidentDbContext and ChangeIntelligenceDbContext
- Time-window based matching (configurable, default 24h before incident)
- Confidence scoring: exact service match = HIGH, dependency match = MEDIUM, time proximity only = LOW
- Persisted correlation results in IncidentDbContext
- Two working API endpoints with proper auth and validation

## 5. Escopo permitido
- **Modules:** `src/modules/operationalintelligence/`
- **Directories:**
  - `NexTraceOne.OperationalIntelligence.Domain/Entities/`
  - `NexTraceOne.OperationalIntelligence.Application/Features/`
  - `NexTraceOne.OperationalIntelligence.Infrastructure/Persistence/`
  - `NexTraceOne.OperationalIntelligence.API/`
- **Types:** Domain entity, CQRS handler, repository, endpoint, unit tests

## 6. Escopo proibido
- Do NOT modify ChangeGovernance module code (read-only access to its DbContext is acceptable)
- Do NOT modify the frontend (that's P01.3)
- Do NOT modify the Catalog module
- Do NOT modify Identity/Audit modules
- Do NOT change existing incident CRUD endpoints
- Do NOT alter EfIncidentStore's existing methods

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Domain/Entities/` — new `IncidentChangeCorrelation.cs` entity
2. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Application/Features/CorrelateIncidentWithChanges/` — new handler
3. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Application/Features/GetCorrelatedChanges/` — new query handler
4. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Persistence/` — repository for correlations
5. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.API/` — endpoint registration
6. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Persistence/IncidentDbContext.cs` — add DbSet for correlation entity
7. `tests/modules/operationalintelligence/` — unit tests
8. `src/modules/changegovernance/NexTraceOne.ChangeGovernance.Infrastructure/Persistence/ChangeIntelligenceDbContext.cs` — READ ONLY reference

## 8. Passos obrigatórios
1. Create `IncidentChangeCorrelation` domain entity with:
   - `Id` (Guid), `IncidentId` (Guid), `ChangeId` (Guid), `ServiceId` (Guid)
   - `ConfidenceLevel` (enum: High, Medium, Low)
   - `CorrelationType` (enum: ExactServiceMatch, DependencyMatch, TimeProximity)
   - `TimeWindowHours` (int), `CorrelatedAt` (DateTimeOffset)
   - Standard audit fields (CreatedAt, CreatedBy, TenantId)
2. Add `DbSet<IncidentChangeCorrelation>` to IncidentDbContext
3. Create `IIncidentCorrelationRepository` with `AddAsync`, `GetByIncidentIdAsync`, `ExistsByIncidentAndChangeAsync`
4. Implement EF Core repository
5. Create `CorrelateIncidentWithChanges` command handler:
   - Accept `IncidentId` and optional `TimeWindowHours` (default 24)
   - Load incident from IncidentDbContext (get service ID, occurred timestamp, environment)
   - Query ChangeIntelligenceDbContext for changes matching: same service, within time window, same environment (or any environment)
   - Score each match: exact service = High, dependency = Medium, time only = Low
   - Persist `IncidentChangeCorrelation` records (skip duplicates)
   - Return list of correlations with change metadata
6. Create `GetCorrelatedChanges` query handler:
   - Accept `IncidentId`
   - Return persisted correlations with change details
7. Register endpoints:
   - `POST /api/v1/incidents/{id}/correlate` → CorrelateIncidentWithChanges (permission: `incidents:write`)
   - `GET /api/v1/incidents/{id}/correlated-changes` → GetCorrelatedChanges (permission: `incidents:read`)
8. Add unit tests:
   - Correlation finds changes within time window
   - Correlation ignores changes outside time window
   - Confidence scoring works correctly
   - Duplicate correlations are skipped
   - Empty result when no matching changes

## 9. Responsabilidades permitidas
- Create new domain entity, handler, repository, endpoint
- Add DbSet to existing IncidentDbContext
- Read from ChangeIntelligenceDbContext (read-only)
- Add unit tests

## 10. Responsabilidades proibidas
- Do NOT modify any existing incident handlers
- Do NOT modify ChangeGovernance module's domain or application layers
- Do NOT modify the frontend
- Do NOT create or modify EF migrations (migration will be separate if needed)

## 11. Critérios de aceite
- [ ] `IncidentChangeCorrelation` entity exists with required properties
- [ ] `CorrelateIncidentWithChanges` handler queries both DbContexts and returns correlations
- [ ] `GetCorrelatedChanges` handler returns persisted correlations
- [ ] `POST /api/v1/incidents/{id}/correlate` endpoint registered with auth
- [ ] `GET /api/v1/incidents/{id}/correlated-changes` endpoint registered with auth
- [ ] Time-window filtering works (changes outside window excluded)
- [ ] Confidence scoring produces High/Medium/Low correctly
- [ ] ≥5 unit tests pass
- [ ] Build succeeds with zero new errors
- [ ] Existing operationalintelligence tests still pass

## 12. Validações obrigatórias
- `dotnet build` succeeds for the OperationalIntelligence projects
- Existing tests in `tests/modules/operationalintelligence/` pass
- New unit tests pass
- No changes to files outside OperationalIntelligence module (except read-only reference)
- CancellationToken used in all async operations
- Result<T> pattern used for handler return types
- Proper guard clauses in handlers

## 13. Riscos e cuidados
- HIGH RISK: Cross-DbContext query — ensure you read ChangeIntelligenceDbContext as a separate dependency, not by coupling the domain layers
- CAUTION: IncidentDbContext may need a new DbSet — verify the model snapshot can accommodate this without migration conflicts
- CAUTION: Tenant isolation must be preserved — correlation should only find changes in the same tenant

## 14. Dependências
- **Depends on:** Nothing (both DbContexts exist with migrations)
- **Unblocks:** P01.3 (frontend needs correlation API), P04.6 (deploy correlation)
- **Blocked until done:** P01.3 cannot start without this

## 15. Entregáveis esperados
- New entity: `IncidentChangeCorrelation`
- New handlers: `CorrelateIncidentWithChanges`, `GetCorrelatedChanges`
- New repository: `IIncidentCorrelationRepository` + EF implementation
- 2 new API endpoints
- ≥5 unit tests
