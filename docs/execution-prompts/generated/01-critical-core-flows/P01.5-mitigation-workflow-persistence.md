# PROMPT — Fix CreateMitigationWorkflow and RecordMitigationValidation to persist data

## 1. Identification
- **Prompt ID:** P01.5
- **Phase:** 01-critical-core-flows
- **Execution order:** 7
- **Type:** fix-implement
- **Area:** backend
- **Module:** operationalintelligence
- **Bounded context:** mitigation
- **Can run in parallel:** no (depends on P01.4)
- **Risk level:** medium

## 2. Objective
Fix `CreateMitigationWorkflow` handler to actually persist `MitigationWorkflowRecord` via repository and IncidentDbContext. Fix `RecordMitigationValidation` handler to persist validation data instead of discarding it. Ensure `GetMitigationHistory` reads persisted records. Link mitigation workflows to RunbookRecord when applicable.

## 3. Problem actual
Per forensic audit: "CreateMitigationWorkflow exists but doesn't persist. RecordMitigationValidation discards data." The handlers construct domain objects but never call `repository.AddAsync()` or `dbContext.SaveChangesAsync()`. `GetMitigationHistory` may still return hardcoded data instead of querying the database. After P01.2 fixes the incident persistence foundation, this prompt completes the mitigation lifecycle by ensuring all mitigation-related writes are real.

## 4. Resultado esperado
- `CreateMitigationWorkflow` persists `MitigationWorkflowRecord` to IncidentDbContext via repository
- `RecordMitigationValidation` persists `MitigationValidationLog` to IncidentDbContext via repository
- `GetMitigationHistory` reads persisted `MitigationWorkflowRecord` entries from database
- Mitigation workflow can optionally link to a RunbookRecord (Guid? RunbookId on MitigationWorkflowRecord)
- Full lifecycle test passes: create incident → create mitigation → record validation → get history → all data persisted

## 5. Escopo permitido
- **Module:** `src/modules/operationalintelligence/`
- **Directories:**
  - `NexTraceOne.OperationalIntelligence.Application/Incidents/Features/CreateMitigationWorkflow/`
  - `NexTraceOne.OperationalIntelligence.Application/Incidents/Features/RecordMitigationValidation/`
  - `NexTraceOne.OperationalIntelligence.Application/Incidents/Features/GetMitigationHistory/`
  - `NexTraceOne.OperationalIntelligence.Application/Incidents/Features/GetMitigationWorkflow/`
  - `NexTraceOne.OperationalIntelligence.Application/Incidents/Abstractions/`
  - `NexTraceOne.OperationalIntelligence.Domain/Incidents/Entities/`
  - `NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/Persistence/`
- **Types:** Handler fixes, repository additions, entity updates, unit tests

## 6. Escopo proibido
- Do NOT modify the frontend
- Do NOT modify other modules (Catalog, ChangeGovernance, AIKnowledge)
- Do NOT modify runbook CRUD logic (that's P01.4)
- Do NOT modify incident correlation logic (that's P01.1)
- Do NOT create or modify EF migrations
- Do NOT change existing API endpoint routes — only fix handler internals

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Application/Incidents/Features/CreateMitigationWorkflow/CreateMitigationWorkflow.cs` — fix to persist
2. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Application/Incidents/Features/RecordMitigationValidation/RecordMitigationValidation.cs` — fix to persist
3. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Application/Incidents/Features/GetMitigationHistory/GetMitigationHistory.cs` — fix to read from DB
4. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Domain/Incidents/Entities/MitigationWorkflowRecord.cs` — verify/add RunbookId property
5. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Domain/Incidents/Entities/MitigationValidationLog.cs` — verify entity completeness
6. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/Persistence/` — verify/extend repositories
7. `tests/modules/operationalintelligence/` — add lifecycle tests

## 8. Passos obrigatórios
1. Open `CreateMitigationWorkflow.cs` handler — identify where `MitigationWorkflowRecord` is constructed but not persisted.
2. Add `await repository.AddAsync(mitigationWorkflowRecord, cancellationToken)` followed by `await dbContext.SaveChangesAsync(cancellationToken)` (or equivalent unit-of-work pattern).
3. Verify `MitigationWorkflowRecord` entity has: `Id`, `IncidentId`, `RunbookId?` (optional link to runbook), `Status`, `CreatedAt`, `CreatedBy`, `TenantId`. Add `RunbookId` if missing.
4. Open `RecordMitigationValidation.cs` handler — identify where validation data is constructed but discarded.
5. Add persistence: create `MitigationValidationLog` entity, persist via repository. Ensure it references the `MitigationWorkflowRecord` ID.
6. Verify `MitigationValidationLog` entity has: `Id`, `MitigationWorkflowId`, `ValidationType`, `ValidationResult`, `Notes`, `ValidatedAt`, `ValidatedBy`, `TenantId`.
7. Open `GetMitigationHistory.cs` handler — replace any hardcoded data return with a query to IncidentDbContext reading `MitigationWorkflowRecord` entries filtered by incident ID, ordered by creation date descending.
8. If repository interfaces for mitigation don't exist in `Application/Incidents/Abstractions/`, create them:
   - `IMitigationWorkflowRepository` with `AddAsync`, `GetByIdAsync`, `GetByIncidentIdAsync`
   - `IMitigationValidationRepository` with `AddAsync`, `GetByWorkflowIdAsync`
9. Implement EF repositories in `Infrastructure/Incidents/Persistence/`.
10. Add unit tests for the mitigation lifecycle:
    - CreateMitigationWorkflow persists record with correct properties
    - CreateMitigationWorkflow with RunbookId links to runbook
    - RecordMitigationValidation persists validation log
    - GetMitigationHistory returns persisted records ordered by date
    - GetMitigationHistory returns empty for incident with no mitigations

## 9. Responsabilidades permitidas
- Fix existing handler logic to call persistence methods
- Add repository interfaces and implementations for mitigation entities
- Add optional RunbookId property to MitigationWorkflowRecord
- Add unit tests for mitigation lifecycle
- Extend EF configurations if needed for new properties

## 10. Responsabilidades proibidas
- Do NOT create new API endpoints (existing ones should work after fixes)
- Do NOT modify runbook CRUD handlers (that's P01.4)
- Do NOT modify the frontend
- Do NOT modify incident creation or correlation handlers
- Do NOT create or modify EF migrations

## 11. Critérios de aceite
- [ ] `CreateMitigationWorkflow` handler persists `MitigationWorkflowRecord` in IncidentDbContext
- [ ] `RecordMitigationValidation` handler persists `MitigationValidationLog` in IncidentDbContext
- [ ] `GetMitigationHistory` returns data from database (not hardcoded)
- [ ] `MitigationWorkflowRecord` has optional `RunbookId` property
- [ ] Repository interfaces exist for mitigation entities
- [ ] EF repository implementations created
- [ ] Full lifecycle test passes: create → validate → retrieve history
- [ ] ≥5 unit tests pass
- [ ] Build succeeds with zero new errors
- [ ] Existing OperationalIntelligence tests still pass

## 12. Validações obrigatórias
- `dotnet build` succeeds for the OperationalIntelligence projects
- Existing tests in `tests/modules/operationalintelligence/` pass
- New unit tests pass
- `git diff` shows changes only in OperationalIntelligence module
- CancellationToken used in all async operations
- Result<T> pattern used for handler return types
- Proper guard clauses in handlers

## 13. Riscos e cuidados
- MEDIUM RISK: Changing handler logic could break existing seed data paths or API consumers expecting specific response shapes
- CAUTION: Ensure MitigationWorkflowRecord and MitigationValidationLog entities have all required properties — if entity changes require migration, document but do not create migration
- CAUTION: Preserve audit fields (CreatedAt, CreatedBy, TenantId) in all persisted records
- CAUTION: If GetMitigationHistory response DTO shape differs from entity, ensure correct mapping

## 14. Dependências
- **Depends on:** P01.4 (RunbookRecord must be persisted so mitigation can link to it via RunbookId)
- **Unblocks:** Nothing directly (but completes the mitigation persistence story started in P01.2)
- **Blocked until done:** Nothing

## 15. Entregáveis esperados
- Fixed `CreateMitigationWorkflow` handler (persists MitigationWorkflowRecord)
- Fixed `RecordMitigationValidation` handler (persists MitigationValidationLog)
- Fixed `GetMitigationHistory` handler (reads from DB)
- Repository interfaces: `IMitigationWorkflowRepository`, `IMitigationValidationRepository` (if not existing)
- EF repository implementations
- ≥5 unit tests for mitigation lifecycle
