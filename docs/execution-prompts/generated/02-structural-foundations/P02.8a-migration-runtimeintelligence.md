# PROMPT — Generate and verify EF Core migration for RuntimeIntelligenceDbContext

## 1. Identification
- **Prompt ID:** P02.8a
- **Phase:** 02-structural-foundations
- **Execution order:** 13
- **Type:** migration
- **Area:** backend
- **Module:** operationalintelligence
- **Bounded context:** runtime-intelligence
- **Can run in parallel:** yes (group-C — with P02.8b, P02.8c, P02.8d, P02.8e, P02.8f, P02.8g)
- **Risk level:** low

## 2. Objective
Verify and validate the EF Core migration for `RuntimeIntelligenceDbContext`. An `InitialCreate` migration already exists (`20260325210556_InitialCreate.cs`). This prompt ensures the migration is correct, the table prefix convention (`ops_`) is respected per `docs/architecture/database-table-prefixes.md`, the migration applies cleanly to a fresh database, and the outbox table (`ops_outbox_messages`) is properly configured.

## 3. Problem actual
`RuntimeIntelligenceDbContext` has an existing InitialCreate migration at `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Migrations/20260325210556_InitialCreate.cs`. However, it has not been systematically verified against the table prefix convention or tested for clean application. The DbContext entities include `RuntimeSnapshot`, `RuntimeBaseline`, and `DriftFinding`, all of which should use the `ops_` prefix (e.g., `ops_runtime_snapshots`, `ops_runtime_baselines`, `ops_drift_findings`). The outbox table should be `ops_outbox_messages` with the standard `idempotency_key` unique index.

## 4. Resultado esperado
- Existing migration verified against table prefix convention (`ops_`)
- All table names use `ops_` prefix consistently
- Outbox table named `ops_outbox_messages` with `idempotency_key` unique index
- Migration applies cleanly with `dotnet ef database update` against a test database
- Model snapshot is consistent with the current entity model
- If any discrepancies are found, a corrective migration is generated

## 5. Escopo permitido
- **Module:** `src/modules/operationalintelligence/`
- **Directories:**
  - `NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Migrations/`
  - `NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/RuntimeIntelligenceDbContext.cs`
  - `NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Configurations/` (entity type configurations)
  - `NexTraceOne.OperationalIntelligence.Domain/Runtime/Entities/`
- **Reference:** `docs/architecture/database-table-prefixes.md`

## 6. Escopo proibido
- Do NOT modify the domain entities
- Do NOT modify the DbContext class (unless OutboxTableName override is incorrect)
- Do NOT modify other module migrations
- Do NOT modify the frontend
- Do NOT modify other DbContexts
- Do NOT delete existing migration files (only add corrective ones if needed)

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Migrations/20260325210556_InitialCreate.cs` — verify table prefixes
2. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Migrations/20260325210556_InitialCreate.Designer.cs` — verify model snapshot
3. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Migrations/RuntimeIntelligenceDbContextModelSnapshot.cs` — verify current snapshot
4. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Runtime/Persistence/Configurations/` — verify entity type configurations use correct table names

## 8. Passos obrigatórios
1. **Read the existing migration file** (`20260325210556_InitialCreate.cs`) and verify:
   - All `migrationBuilder.CreateTable()` calls use `ops_` prefixed table names
   - Column names follow snake_case convention
   - Primary keys, foreign keys, and indexes are correctly defined
   - The outbox table is named `ops_outbox_messages` (not the default `outbox_messages`)
2. **Verify entity type configurations** in `Configurations/` directory:
   - Each entity configuration specifies `ToTable("ops_entity_name")`
   - Column mappings are explicit and use snake_case
3. **Verify DbContext `OutboxTableName` override:**
   - `RuntimeIntelligenceDbContext` should override `OutboxTableName` to return `"ops_outbox_messages"`
   - If missing, add the override
4. **Verify the model snapshot** is consistent:
   - Run `dotnet ef migrations has-pending-model-changes --context RuntimeIntelligenceDbContext --project src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/`
   - If pending changes exist, generate a corrective migration
5. **Test migration application:**
   - Run `dotnet ef database update --context RuntimeIntelligenceDbContext --connection "Host=localhost;Database=nextraceone_test_runtime;..."` (or use a test connection string)
   - Verify all tables are created with correct names and structure
6. **Verify `idempotency_key` unique index** exists on the outbox table

## 9. Responsabilidades permitidas
- Verify existing migration content
- Add corrective migration if prefix issues found
- Add or fix `OutboxTableName` override in DbContext if needed
- Verify entity type configurations
- Run migration commands

## 10. Responsabilidades proibidas
- Do NOT delete existing migration files
- Do NOT modify domain entities
- Do NOT modify NexTraceDbContextBase
- Do NOT modify other module migrations or DbContexts
- Do NOT modify the frontend

## 11. Critérios de aceite
- [ ] All tables in RuntimeIntelligenceDbContext use `ops_` prefix
- [ ] Outbox table is named `ops_outbox_messages`
- [ ] `idempotency_key` unique index exists on outbox table
- [ ] Model snapshot is consistent (no pending model changes)
- [ ] Migration applies cleanly (no errors)
- [ ] Entity type configurations match migration output
- [ ] Build succeeds with zero new errors

## 12. Validações obrigatórias
- `dotnet build` succeeds for OperationalIntelligence.Infrastructure project
- `dotnet ef migrations has-pending-model-changes` reports no pending changes (or corrective migration added)
- Table prefix convention matches `docs/architecture/database-table-prefixes.md`
- No changes to other module files

## 13. Riscos e cuidados
- **LOW RISK:** This is a verification and potential correction task — the migration already exists
- **CAUTION:** If table prefixes are wrong in the existing migration, a corrective migration that renames tables is needed — this must be carefully tested to avoid data loss
- **CAUTION:** Running `dotnet ef database update` requires a database connection — use a dedicated test database or `--dry-run` if available
- **CAUTION:** Do not regenerate the InitialCreate migration (delete + recreate) — this would break any environment that already applied it

## 14. Dependências
- **Depends on:** Nothing
- **Unblocks:** P02.5 (IRuntimeIntelligenceModule implementation needs a verified migration)
- **Can run in parallel with:** P02.8b, P02.8c, P02.8d, P02.8e, P02.8f, P02.8g (group-C)

## 15. Entregáveis esperados
- Verified migration file with correct `ops_` table prefixes
- Verified outbox table configuration (`ops_outbox_messages` with `idempotency_key` index)
- Corrective migration (if prefix issues found)
- Verification report: table names, prefix compliance, model snapshot consistency
