# PROMPT — Activate outbox event processing and consumer registration for all module DbContexts

## 1. Identification
- **Prompt ID:** P02.1
- **Phase:** 02-structural-foundations
- **Execution order:** 20
- **Type:** feature-implement
- **Area:** backend
- **Module:** platform
- **Bounded context:** outbox
- **Can run in parallel:** no
- **Risk level:** medium

## 2. Objective
Activate outbox event processing and integration event consumer registration for CatalogGraphDbContext, ChangeIntelligenceDbContext, and IncidentDbContext. Currently, while `ModuleOutboxProcessorJob<TContext>` is registered as a hosted service for all DbContexts in BackgroundWorkers/Program.cs (lines 122–152), the event consumers that handle the deserialized integration events are not fully wired. The IdentityAccess module is the reference implementation that has working end-to-end outbox→consumer flow. Additionally, three modules (Integrations, Knowledge, ProductAnalytics) have their infrastructure not registered in BackgroundWorkers at all — their outbox processors must also be added.

## 3. Problem actual
The outbox pattern infrastructure exists: `ModuleOutboxProcessorJob<TContext>` (173 lines in `BackgroundWorkers/Jobs/ModuleOutboxProcessorJob.cs`) picks up `OutboxMessage` records from each DbContext, deserializes them into integration events, and publishes via `IEventBus`. However:

1. **Event consumers are sparse:** Only the Notifications module has `IIntegrationEventHandler<TEvent>` implementations (in `Notifications.Infrastructure/EventHandlers/`). CatalogGraph, ChangeIntelligence, and Incidents modules publish integration events (defined in their respective `*.Contracts/IntegrationEvents/`) but no consumers in other modules handle them.
2. **Three DbContexts missing from BackgroundWorkers:** IntegrationsDbContext, KnowledgeDbContext, and ProductAnalyticsDbContext are NOT registered as `ModuleOutboxProcessorJob<T>` in BackgroundWorkers/Program.cs, despite their DbContexts inheriting from `NexTraceDbContextBase` (which includes outbox support). Their `Add{Module}Infrastructure()` methods are also not called in BackgroundWorkers/Program.cs.
3. **No cross-module event propagation:** When a catalog service is updated, a change is recorded, or an incident is created, no downstream modules react. This blocks change intelligence correlation, service reliability updates, and knowledge capture workflows.

## 4. Resultado esperado
- Integration event consumers registered for critical cross-module events from Catalog, ChangeIntelligence, and Incidents
- IntegrationsDbContext, KnowledgeDbContext, and ProductAnalyticsDbContext added to BackgroundWorkers outbox processing pipeline
- Health checks added for the three new outbox processors
- At least one integration test verifying end-to-end: domain event → outbox → processor → consumer
- IdentityAccess outbox flow used as reference pattern (not modified)

## 5. Escopo permitido
- **Platform:** `src/platform/NexTraceOne.BackgroundWorkers/`
- **Modules (consumer creation):**
  - `src/modules/catalog/NexTraceOne.Catalog.Infrastructure/`
  - `src/modules/changegovernance/NexTraceOne.ChangeGovernance.Infrastructure/`
  - `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/`
  - `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/`
  - `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/`
  - `src/modules/productanalytics/NexTraceOne.ProductAnalytics.Infrastructure/`
- **Building blocks (read-only reference):**
  - `src/building-blocks/NexTraceOne.BuildingBlocks.Infrastructure/Outbox/`
  - `src/building-blocks/NexTraceOne.BuildingBlocks.Infrastructure/EventBus/`
- **Tests:** `tests/` — integration tests for outbox flow

## 6. Escopo proibido
- Do NOT modify `ModuleOutboxProcessorJob<TContext>` logic — it works correctly
- Do NOT modify `NexTraceDbContextBase` outbox infrastructure
- Do NOT modify `OutboxMessage` entity
- Do NOT modify the IdentityAccess module (use as read-only reference)
- Do NOT modify the frontend
- Do NOT modify existing integration event definitions in Contracts projects
- Do NOT create or modify EF migrations
- Do NOT change the Quartz.NET scheduling configuration

## 7. Ficheiros principais candidatos a alteração
1. `src/platform/NexTraceOne.BackgroundWorkers/Program.cs` — add IntegrationsDbContext, KnowledgeDbContext, ProductAnalyticsDbContext infrastructure registration and outbox processor hosted services; add health checks for the three new processors
2. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/EventHandlers/` — new consumer for `DeploymentEventReceivedEvent` from ChangeGovernance (to correlate incidents with changes)
3. `src/modules/changegovernance/NexTraceOne.ChangeGovernance.Infrastructure/ChangeIntelligence/EventHandlers/` — new consumer for incident-related events from OperationalIntelligence
4. `src/modules/catalog/NexTraceOne.Catalog.Infrastructure/Graph/EventHandlers/` — new consumer for change events that affect catalog services
5. `src/modules/notifications/NexTraceOne.Notifications.Infrastructure/EventHandlers/` — verify existing handlers cover the events being activated (read-only check)
6. `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/DependencyInjection.cs` — verify infrastructure registration method exists and is callable from BackgroundWorkers
7. `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/DependencyInjection.cs` — same verification
8. `src/modules/productanalytics/NexTraceOne.ProductAnalytics.Infrastructure/DependencyInjection.cs` — same verification

## 8. Passos obrigatórios
1. **Study the reference implementation:** Read IdentityAccess outbox processing flow end-to-end:
   - How identity integration events are defined in `NexTraceOne.IdentityAccess.Contracts/IntegrationEvents/`
   - How they are published via domain events → outbox in `IdentityDbContext`
   - How `ModuleOutboxProcessorJob<IdentityDbContext>` picks them up
   - How consumers in Notifications module handle them via `IIntegrationEventHandler<TEvent>`
2. **Register missing infrastructure in BackgroundWorkers/Program.cs:**
   - Add `builder.Services.AddIntegrationsInfrastructure(builder.Configuration);`
   - Add `builder.Services.AddKnowledgeInfrastructure(builder.Configuration);`
   - Add `builder.Services.AddProductAnalyticsInfrastructure(builder.Configuration);`
   - Add `builder.Services.AddHostedService<ModuleOutboxProcessorJob<IntegrationsDbContext>>();`
   - Add `builder.Services.AddHostedService<ModuleOutboxProcessorJob<KnowledgeDbContext>>();`
   - Add `builder.Services.AddHostedService<ModuleOutboxProcessorJob<ProductAnalyticsDbContext>>();`
3. **Add health checks for the three new outbox processors** following the existing pattern in BackgroundWorkers/Program.cs (lines 88–107)
4. **Create event consumers for critical cross-module events:**
   - OperationalIntelligence consuming `ReleasePublishedEvent` and `DeploymentEventReceivedEvent` from ChangeGovernance
   - ChangeGovernance consuming `IncidentCreatedEvent` from OperationalIntelligence (if the event exists in contracts)
   - Catalog consuming change-related events to update service health metadata
5. **Register consumers in DI** within each module's `DependencyInjection.cs`
6. **Add integration tests** verifying:
   - OutboxMessage is created when a domain event fires
   - ModuleOutboxProcessorJob picks up and processes the message
   - Consumer receives the deserialized integration event
7. **Verify idempotency:** Ensure `idempotency_key` unique index prevents duplicate processing

## 9. Responsabilidades permitidas
- Register three missing module infrastructures in BackgroundWorkers
- Add three missing outbox processor hosted services
- Add three missing health checks
- Create new `IIntegrationEventHandler<TEvent>` implementations in appropriate modules
- Register new consumers in module DI
- Add integration tests for the outbox pipeline

## 10. Responsabilidades proibidas
- Do NOT rewrite the outbox processor job
- Do NOT change the outbox message schema
- Do NOT modify NexTraceDbContextBase
- Do NOT modify existing integration event definitions
- Do NOT modify the Identity module
- Do NOT add new integration event types (only consume existing ones)
- Do NOT modify the frontend

## 11. Critérios de aceite
- [ ] IntegrationsDbContext, KnowledgeDbContext, ProductAnalyticsDbContext registered as `ModuleOutboxProcessorJob<T>` in BackgroundWorkers/Program.cs
- [ ] Infrastructure registration methods called for the three new modules in BackgroundWorkers/Program.cs
- [ ] Health checks added for the three new outbox processors
- [ ] At least 2 cross-module event consumers created (e.g., OperationalIntelligence consuming ChangeGovernance events, and vice versa)
- [ ] Consumers implement `IIntegrationEventHandler<TEvent>` pattern
- [ ] Consumers registered in module DI
- [ ] ≥3 integration tests pass covering outbox→consumer flow
- [ ] Build succeeds with zero new errors
- [ ] Existing tests pass unchanged

## 12. Validações obrigatórias
- `dotnet build` succeeds for BackgroundWorkers, affected Infrastructure projects, and test projects
- Existing tests in `tests/` pass
- New integration tests pass
- No changes to IdentityAccess module files
- No changes to NexTraceDbContextBase or OutboxMessage
- CancellationToken used in all async operations in consumers
- Event handler methods are idempotent (handle duplicate delivery gracefully)

## 13. Riscos e cuidados
- **MEDIUM RISK:** Adding three new hosted services to BackgroundWorkers increases resource usage — ensure they follow the same batch/interval pattern (50 messages, 5s cycle) as existing processors
- **CAUTION:** IntegrationsDbContext, KnowledgeDbContext, and ProductAnalyticsDbContext may not have migrations yet — the outbox processor will fail if the outbox table doesn't exist. Ensure P02.8e, P02.8f, P02.8g migrations run first or handle the missing table gracefully
- **CAUTION:** Cross-module consumers must not create tight coupling — consumers should handle events defensively and tolerate missing data
- **CAUTION:** Integration events use `Type.GetType()` for deserialization — ensure event type names are resolvable across assembly boundaries
- **CAUTION:** Tenant isolation must be preserved in all consumer logic

## 14. Dependências
- **Depends on:** Nothing (outbox infrastructure already exists). However, P02.8e/f/g migrations should ideally run first for the three modules missing migrations
- **Unblocks:** Cross-module event propagation (incident↔change correlation, service catalog updates on change, knowledge capture triggers)
- **Soft dependency:** Consumers for ChangeGovernance events benefit from P02.3 (IChangeIntelligenceModule), but can be implemented independently

## 15. Entregáveis esperados
- Modified `BackgroundWorkers/Program.cs` with 3 new infrastructure registrations, 3 new outbox processor hosted services, 3 new health checks
- ≥2 new `IIntegrationEventHandler<TEvent>` consumer implementations in appropriate module Infrastructure projects
- Consumer DI registration in affected modules
- ≥3 integration tests for outbox→consumer pipeline
- Documentation comment in Program.cs explaining which modules have outbox processing active
