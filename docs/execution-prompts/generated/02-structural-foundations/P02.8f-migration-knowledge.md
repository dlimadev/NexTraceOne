# PROMPT — Generate EF Core InitialCreate migration for KnowledgeDbContext

## 1. Identification
- **Prompt ID:** P02.8f
- **Phase:** 02-structural-foundations
- **Execution order:** 18
- **Type:** migration
- **Area:** backend
- **Module:** knowledge
- **Bounded context:** knowledge
- **Can run in parallel:** yes (group-C — with P02.8a, P02.8b, P02.8c, P02.8d, P02.8e, P02.8g)
- **Risk level:** low

## 2. Objective
Generate the EF Core `InitialCreate` migration for `KnowledgeDbContext`. This DbContext has **no existing migrations** — the `Migrations/` directory is empty. The migration must be created from scratch, respecting the `knw_` table prefix convention per `docs/architecture/database-table-prefixes.md`. The Knowledge module is the Knowledge Hub of NexTraceOne, containing operational documents, notes, and knowledge relations.

## 3. Problem actual
`KnowledgeDbContext` exists at `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/Persistence/KnowledgeDbContext.cs` with three entities:
- `KnowledgeDocument` — operational and technical knowledge documents
- `OperationalNote` — persisted operational notes
- `KnowledgeRelation` — relations between knowledge objects and other contexts (services, changes, incidents)

The `Migrations/` directory at `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/Persistence/Migrations/` is empty. The DbContext comment explicitly states `Prefixo de tabelas: knw_`. The outbox table should be `knw_outbox_messages`.

Additionally, the Knowledge module infrastructure is NOT registered in `BackgroundWorkers/Program.cs`, meaning outbox processing cannot start until P02.1 adds it.

## 4. Resultado esperado
- InitialCreate migration generated for KnowledgeDbContext
- All table names use `knw_` prefix (e.g., `knw_knowledge_documents`, `knw_operational_notes`, `knw_knowledge_relations`)
- Outbox table named `knw_outbox_messages` with `idempotency_key` unique index
- Migration applies cleanly to a fresh database
- Model snapshot generated and consistent

## 5. Escopo permitido
- **Module:** `src/modules/knowledge/`
- **Directories:**
  - `NexTraceOne.Knowledge.Infrastructure/Persistence/Migrations/` — create migration files
  - `NexTraceOne.Knowledge.Infrastructure/Persistence/KnowledgeDbContext.cs` — verify OutboxTableName override
  - `NexTraceOne.Knowledge.Infrastructure/Persistence/Configurations/` — verify or create entity type configurations
  - `NexTraceOne.Knowledge.Domain/Entities/` — read-only reference
- **Reference:** `docs/architecture/database-table-prefixes.md`

## 6. Escopo proibido
- Do NOT modify the domain entities
- Do NOT modify other module migrations or DbContexts
- Do NOT modify the frontend
- Do NOT modify BackgroundWorkers registration (that's P02.1)
- Do NOT modify Contracts project

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/Persistence/Migrations/` — new InitialCreate migration files (migration .cs, Designer.cs, ModelSnapshot.cs)
2. `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/Persistence/KnowledgeDbContext.cs` — verify/add OutboxTableName override to `"knw_outbox_messages"`
3. `src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/Persistence/Configurations/` — verify or create entity type configurations with `knw_` table names

## 8. Passos obrigatórios
1. **Verify entity type configurations exist** in `Persistence/Configurations/`:
   - `KnowledgeDocumentConfiguration` with `ToTable("knw_knowledge_documents")`
   - `OperationalNoteConfiguration` with `ToTable("knw_operational_notes")`
   - `KnowledgeRelationConfiguration` with `ToTable("knw_knowledge_relations")`
   - If configurations don't exist, create them following the pattern from other modules
2. **Verify DbContext `OutboxTableName` override:**
   - `KnowledgeDbContext` must override `OutboxTableName` to return `"knw_outbox_messages"`
   - If missing, add the override
3. **Verify `ConfigurationsAssembly` override** is set so EF Core discovers the entity type configurations
4. **Generate the migration:**
   ```bash
   dotnet ef migrations add InitialCreate \
     --context KnowledgeDbContext \
     --project src/modules/knowledge/NexTraceOne.Knowledge.Infrastructure/ \
     --startup-project src/platform/NexTraceOne.ApiHost/
   ```
5. **Verify the generated migration:**
   - All table names have `knw_` prefix
   - Column names follow snake_case
   - Primary keys, foreign keys, indexes correct
   - Outbox table is `knw_outbox_messages` with `idempotency_key` unique index
   - `KnowledgeRelation` has proper foreign key or reference fields for cross-context linking
6. **Test migration application** against test database
7. **Verify model snapshot** is complete and accurate

## 9. Responsabilidades permitidas
- Create entity type configurations if missing
- Add OutboxTableName override if missing
- Add ConfigurationsAssembly override if missing
- Generate InitialCreate migration
- Verify and fix table names

## 10. Responsabilidades proibidas
- Do NOT modify domain entities
- Do NOT modify NexTraceDbContextBase
- Do NOT modify other module migrations
- Do NOT modify BackgroundWorkers
- Do NOT modify the frontend

## 11. Critérios de aceite
- [ ] InitialCreate migration generated in `Persistence/Migrations/`
- [ ] All tables use `knw_` prefix
- [ ] Outbox table named `knw_outbox_messages`
- [ ] `idempotency_key` unique index exists on outbox table
- [ ] `KnowledgeDocument`, `OperationalNote`, `KnowledgeRelation` tables created with correct columns
- [ ] Model snapshot generated and consistent
- [ ] Migration applies cleanly
- [ ] Build succeeds with zero new errors

## 12. Validações obrigatórias
- `dotnet build` succeeds for Knowledge.Infrastructure project
- `dotnet ef migrations has-pending-model-changes --context KnowledgeDbContext` reports no pending changes after migration generation
- Table prefix convention matches `docs/architecture/database-table-prefixes.md` (`knw_`)
- No changes to other module files

## 13. Riscos e cuidados
- **LOW RISK:** Fresh migration creation — no existing data to migrate
- **CAUTION:** `KnowledgeRelation` may have polymorphic references (linking to services, changes, incidents by type + ID) — verify the entity structure supports this without cross-module foreign keys
- **CAUTION:** Entity type configurations may not exist yet — create before generating migration
- **CAUTION:** The `--startup-project` must resolve DI correctly — use ApiHost
- **CAUTION:** `KnowledgeDocument` may have large text fields (content) — ensure the column type supports this (e.g., `text` not `varchar(255)`)

## 14. Dependências
- **Depends on:** Nothing (DbContext and entities exist)
- **Unblocks:** P03.5 (Knowledge Hub features), P02.1 (outbox processing for Knowledge module)
- **Can run in parallel with:** P02.8a, P02.8b, P02.8c, P02.8d, P02.8e, P02.8g (group-C)

## 15. Entregáveis esperados
- New migration files: `InitialCreate.cs`, `InitialCreate.Designer.cs`, `KnowledgeDbContextModelSnapshot.cs`
- Verified entity type configurations with `knw_` table prefix
- Verified `OutboxTableName` override (`knw_outbox_messages`)
- Migration applies cleanly report
