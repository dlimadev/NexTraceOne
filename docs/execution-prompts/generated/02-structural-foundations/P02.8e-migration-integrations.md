# PROMPT — Generate EF Core InitialCreate migration for IntegrationsDbContext

## 1. Identification
- **Prompt ID:** P02.8e
- **Phase:** 02-structural-foundations
- **Execution order:** 17
- **Type:** migration
- **Area:** backend
- **Module:** integrations
- **Bounded context:** integrations
- **Can run in parallel:** yes (group-C — with P02.8a, P02.8b, P02.8c, P02.8d, P02.8f, P02.8g)
- **Risk level:** low

## 2. Objective
Generate the EF Core `InitialCreate` migration for `IntegrationsDbContext`. Unlike the previous P02.8a–d prompts, this DbContext has **no existing migrations** — the `Migrations/` directory is empty. The migration must be created from scratch, respecting the `int_` table prefix convention per `docs/architecture/database-table-prefixes.md`. The Integrations module contains entities extracted from the former GovernanceDbContext in P2.1 and P2.2 (IntegrationConnector, IngestionSource, IngestionExecution).

## 3. Problem actual
`IntegrationsDbContext` exists at `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/Persistence/IntegrationsDbContext.cs` with three entities:
- `IntegrationConnector` — connectors for external systems (extracted from GovernanceDbContext in P2.1)
- `IngestionSource` — data ingestion sources (extracted in P2.2)
- `IngestionExecution` — ingestion execution records (extracted in P2.2)

The `Migrations/` directory at `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/Persistence/Migrations/` is empty — no migration files exist. The outbox table should be named `int_outbox_messages`. Entity type configurations should use the `int_` prefix for all table names.

Additionally, the Integrations module infrastructure is NOT registered in `BackgroundWorkers/Program.cs`, meaning outbox processing cannot start until P02.1 adds it.

## 4. Resultado esperado
- InitialCreate migration generated for IntegrationsDbContext
- All table names use `int_` prefix (e.g., `int_integration_connectors`, `int_ingestion_sources`, `int_ingestion_executions`)
- Outbox table named `int_outbox_messages` with `idempotency_key` unique index
- Migration applies cleanly to a fresh database
- Model snapshot generated and consistent
- Entity type configurations verified or created with correct table names

## 5. Escopo permitido
- **Module:** `src/modules/integrations/`
- **Directories:**
  - `NexTraceOne.Integrations.Infrastructure/Persistence/Migrations/` — create migration files
  - `NexTraceOne.Integrations.Infrastructure/Persistence/IntegrationsDbContext.cs` — verify OutboxTableName override
  - `NexTraceOne.Integrations.Infrastructure/Persistence/Configurations/` — verify or create entity type configurations
  - `NexTraceOne.Integrations.Domain/Entities/` — read-only reference
- **Reference:** `docs/architecture/database-table-prefixes.md`

## 6. Escopo proibido
- Do NOT modify the domain entities
- Do NOT modify other module migrations or DbContexts
- Do NOT modify the frontend
- Do NOT modify BackgroundWorkers registration (that's P02.1)
- Do NOT modify the Contracts project (integration events are already defined)

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/Persistence/Migrations/` — new InitialCreate migration files (migration .cs, Designer.cs, ModelSnapshot.cs)
2. `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/Persistence/IntegrationsDbContext.cs` — verify/add OutboxTableName override to `"int_outbox_messages"`
3. `src/modules/integrations/NexTraceOne.Integrations.Infrastructure/Persistence/Configurations/` — verify or create entity type configurations with `int_` table names

## 8. Passos obrigatórios
1. **Verify entity type configurations exist** in `Persistence/Configurations/`:
   - `IntegrationConnectorConfiguration` with `ToTable("int_integration_connectors")`
   - `IngestionSourceConfiguration` with `ToTable("int_ingestion_sources")`
   - `IngestionExecutionConfiguration` with `ToTable("int_ingestion_executions")` (if entity exists as DbSet)
   - If configurations don't exist, create them following the pattern from other modules
2. **Verify DbContext `OutboxTableName` override:**
   - `IntegrationsDbContext` must override `OutboxTableName` to return `"int_outbox_messages"`
   - If missing, add the override
3. **Generate the migration:**
   ```bash
   dotnet ef migrations add InitialCreate \
     --context IntegrationsDbContext \
     --project src/modules/integrations/NexTraceOne.Integrations.Infrastructure/ \
     --startup-project src/platform/NexTraceOne.ApiHost/
   ```
4. **Verify the generated migration:**
   - All table names have `int_` prefix
   - Column names follow snake_case
   - Primary keys, foreign keys, indexes correct
   - Outbox table is `int_outbox_messages` with `idempotency_key` unique index
5. **Test migration application:**
   - `dotnet ef database update --context IntegrationsDbContext` against a test database
6. **Verify model snapshot** is complete and accurate

## 9. Responsabilidades permitidas
- Create entity type configurations if missing
- Add OutboxTableName override if missing
- Generate InitialCreate migration
- Verify and fix table names
- Run migration commands

## 10. Responsabilidades proibidas
- Do NOT modify domain entities
- Do NOT modify NexTraceDbContextBase
- Do NOT modify other module migrations
- Do NOT modify BackgroundWorkers (P02.1 handles that)
- Do NOT modify the frontend
- Do NOT modify integration event definitions

## 11. Critérios de aceite
- [ ] InitialCreate migration generated in `Persistence/Migrations/`
- [ ] All tables use `int_` prefix
- [ ] Outbox table named `int_outbox_messages`
- [ ] `idempotency_key` unique index exists on outbox table
- [ ] `IntegrationConnector`, `IngestionSource` tables created with correct columns
- [ ] Model snapshot generated and consistent
- [ ] Migration applies cleanly
- [ ] Build succeeds with zero new errors

## 12. Validações obrigatórias
- `dotnet build` succeeds for Integrations.Infrastructure project
- `dotnet ef migrations has-pending-model-changes --context IntegrationsDbContext` reports no pending changes after migration generation
- Table prefix convention matches `docs/architecture/database-table-prefixes.md` (`int_`)
- No changes to other module files

## 13. Riscos e cuidados
- **LOW RISK:** Fresh migration creation — no existing data to migrate
- **CAUTION:** Entity type configurations may not exist yet — check the `Configurations/` directory first. If missing, create them before generating the migration
- **CAUTION:** The `IngestionExecution` entity may or may not be a DbSet on IntegrationsDbContext — check if it's configured as an owned entity or a separate table
- **CAUTION:** The `--startup-project` must be ApiHost to resolve connection strings and DI — ensure the command uses the correct startup project
- **CAUTION:** If the `ConfigurationsAssembly` override is not set, EF Core may not find the configurations — verify the DbContext includes them

## 14. Dependências
- **Depends on:** Nothing (DbContext and entities exist)
- **Unblocks:** P04.4 (integrations features), P02.1 (outbox processing for Integrations module — needs migration to create outbox table)
- **Can run in parallel with:** P02.8a, P02.8b, P02.8c, P02.8d, P02.8f, P02.8g (group-C)

## 15. Entregáveis esperados
- New migration files: `InitialCreate.cs`, `InitialCreate.Designer.cs`, `IntegrationsDbContextModelSnapshot.cs`
- Verified entity type configurations with `int_` table prefix
- Verified `OutboxTableName` override (`int_outbox_messages`)
- Migration applies cleanly report
