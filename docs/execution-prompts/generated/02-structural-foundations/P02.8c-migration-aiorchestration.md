# PROMPT — Generate and verify EF Core migration for AiOrchestrationDbContext

## 1. Identification
- **Prompt ID:** P02.8c
- **Phase:** 02-structural-foundations
- **Execution order:** 15
- **Type:** migration
- **Area:** backend
- **Module:** aiknowledge
- **Bounded context:** ai-orchestration
- **Can run in parallel:** yes (group-C — with P02.8a, P02.8b, P02.8d, P02.8e, P02.8f, P02.8g)
- **Risk level:** low

## 2. Objective
Verify and validate the EF Core migration for `AiOrchestrationDbContext`. An `InitialCreate` migration already exists (`20260325211330_InitialCreate.cs`). This prompt ensures the migration is correct, the table prefix convention (`aik_`) is respected per `docs/architecture/database-table-prefixes.md`, the migration applies cleanly, and the outbox table (`aik_outbox_messages`) is properly configured.

## 3. Problem actual
`AiOrchestrationDbContext` has an existing InitialCreate migration at `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Migrations/20260325211330_InitialCreate.cs`. The DbContext entities include `AiContext`, `AiConversation`, `GeneratedTestArtifact`, and `KnowledgeSuggestion`. All tables should use the `aik_` prefix (e.g., `aik_ai_contexts`, `aik_ai_conversations`, `aik_generated_test_artifacts`, `aik_knowledge_suggestions`). The outbox table should be `aik_outbox_messages`. After P01.7 (conversation persistence), this DbContext becomes critical for AI conversation storage.

## 4. Resultado esperado
- Existing migration verified against table prefix convention (`aik_`)
- All table names use `aik_` prefix consistently
- Outbox table named `aik_outbox_messages` with `idempotency_key` unique index
- Migration applies cleanly
- Model snapshot is consistent
- If discrepancies are found, a corrective migration is generated

## 5. Escopo permitido
- **Module:** `src/modules/aiknowledge/`
- **Directories:**
  - `NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Migrations/`
  - `NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/AiOrchestrationDbContext.cs`
  - `NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Configurations/`
  - `NexTraceOne.AIKnowledge.Domain/Orchestration/Entities/`
- **Reference:** `docs/architecture/database-table-prefixes.md`

## 6. Escopo proibido
- Do NOT modify the domain entities
- Do NOT modify the DbContext class (unless OutboxTableName override is incorrect)
- Do NOT modify other module migrations
- Do NOT modify the frontend
- Do NOT modify other DbContexts
- Do NOT delete existing migration files

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Migrations/20260325211330_InitialCreate.cs` — verify table prefixes
2. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Migrations/20260325211330_InitialCreate.Designer.cs` — verify model snapshot
3. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Migrations/AiOrchestrationDbContextModelSnapshot.cs` — verify current snapshot
4. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/Orchestration/Persistence/Configurations/` — verify entity type configurations

## 8. Passos obrigatórios
1. **Read the existing migration file** and verify:
   - All `CreateTable` calls use `aik_` prefixed table names
   - `AiContext`, `AiConversation`, `GeneratedTestArtifact`, `KnowledgeSuggestion` tables exist
   - Column names follow snake_case convention
   - Outbox table is `aik_outbox_messages`
2. **Verify entity type configurations** use correct `ToTable("aik_...")` calls
3. **Verify DbContext `OutboxTableName` override** returns `"aik_outbox_messages"`
4. **Check model snapshot consistency:**
   - `dotnet ef migrations has-pending-model-changes --context AiOrchestrationDbContext --project src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/`
5. **Test migration application** against test database
6. **Verify `idempotency_key` unique index** on outbox table

## 9. Responsabilidades permitidas
- Verify existing migration content
- Add corrective migration if prefix issues found
- Add or fix `OutboxTableName` override if needed
- Run migration commands

## 10. Responsabilidades proibidas
- Do NOT delete existing migration files
- Do NOT modify domain entities
- Do NOT modify NexTraceDbContextBase
- Do NOT modify other module migrations
- Do NOT modify the frontend

## 11. Critérios de aceite
- [ ] All tables in AiOrchestrationDbContext use `aik_` prefix
- [ ] Outbox table is named `aik_outbox_messages`
- [ ] `idempotency_key` unique index exists on outbox table
- [ ] Model snapshot is consistent (no pending model changes)
- [ ] Migration applies cleanly
- [ ] Entity type configurations match migration output
- [ ] Build succeeds with zero new errors

## 12. Validações obrigatórias
- `dotnet build` succeeds for AIKnowledge.Infrastructure project
- `dotnet ef migrations has-pending-model-changes` reports no pending changes
- Table prefix convention matches `docs/architecture/database-table-prefixes.md`
- No changes to other module files

## 13. Riscos e cuidados
- **LOW RISK:** Verification task — migration already exists
- **CAUTION:** AiConversation entity may be modified by P01.7 (conversation persistence) — if P01.7 adds fields, a new migration will be needed. This prompt verifies the baseline
- **CAUTION:** If `KnowledgeSuggestion` table needs fields not yet in the entity, note the gap but do not modify the entity

## 14. Dependências
- **Depends on:** Nothing
- **Unblocks:** P02.6 (IAiOrchestrationModule implementation needs verified migration)
- **Can run in parallel with:** P02.8a, P02.8b, P02.8d, P02.8e, P02.8f, P02.8g (group-C)

## 15. Entregáveis esperados
- Verified migration file with correct `aik_` table prefixes
- Verified outbox table configuration
- Corrective migration (if issues found)
- Verification report: table names, prefix compliance, model snapshot consistency
