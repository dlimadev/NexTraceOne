# PROMPT — Generate and verify EF Core migration for ExternalAiDbContext

## 1. Identification
- **Prompt ID:** P02.8d
- **Phase:** 02-structural-foundations
- **Execution order:** 16
- **Type:** migration
- **Area:** backend
- **Module:** aiknowledge
- **Bounded context:** external-ai
- **Can run in parallel:** yes (group-C — with P02.8a, P02.8b, P02.8c, P02.8e, P02.8f, P02.8g)
- **Risk level:** low

## 2. Objective
Verify and validate the EF Core migration for `ExternalAiDbContext`. An `InitialCreate` migration already exists (`20260325211312_InitialCreate.cs`). This prompt ensures the migration is correct, the table prefix convention (`aik_`) is respected per `docs/architecture/database-table-prefixes.md`, the migration applies cleanly, and the outbox table is properly configured.

## 3. Problem actual
`ExternalAiDbContext` has an existing InitialCreate migration at `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Migrations/20260325211312_InitialCreate.cs`. The DbContext entities include `ExternalAiProvider`, `ExternalAiPolicy`, `ExternalAiConsultation`, and `KnowledgeCapture`. All tables should use the `aik_` prefix (e.g., `aik_external_ai_providers`, `aik_external_ai_policies`, `aik_external_ai_consultations`, `aik_knowledge_captures`). The outbox table should be `aik_outbox_messages`.

Note: Both ExternalAiDbContext and AiOrchestrationDbContext share the `aik_` prefix because they belong to the same AIKnowledge module. If they share a database, the outbox table name must be unique — verify if each has a distinct outbox table name or if they share one.

## 4. Resultado esperado
- Existing migration verified against table prefix convention (`aik_`)
- All table names use `aik_` prefix consistently
- Outbox table properly named and unique (no conflict with AiOrchestrationDbContext outbox)
- `idempotency_key` unique index exists on outbox table
- Migration applies cleanly
- Model snapshot is consistent

## 5. Escopo permitido
- **Module:** `src/modules/aiknowledge/`
- **Directories:**
  - `NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Migrations/`
  - `NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/ExternalAiDbContext.cs`
  - `NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Configurations/`
  - `NexTraceOne.AIKnowledge.Domain/ExternalAI/Entities/`
- **Reference:** `docs/architecture/database-table-prefixes.md`

## 6. Escopo proibido
- Do NOT modify the domain entities
- Do NOT modify the DbContext class (unless OutboxTableName override is incorrect)
- Do NOT modify other module migrations (including AiOrchestrationDbContext)
- Do NOT modify the frontend
- Do NOT delete existing migration files

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Migrations/20260325211312_InitialCreate.cs` — verify table prefixes
2. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Migrations/20260325211312_InitialCreate.Designer.cs` — verify model snapshot
3. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Migrations/ExternalAiDbContextModelSnapshot.cs` — verify current snapshot
4. `src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/ExternalAI/Persistence/Configurations/` — verify entity type configurations

## 8. Passos obrigatórios
1. **Read the existing migration file** and verify:
   - All `CreateTable` calls use `aik_` prefixed table names
   - `ExternalAiProvider`, `ExternalAiPolicy`, `ExternalAiConsultation`, `KnowledgeCapture` tables exist
   - Column names follow snake_case convention
   - Outbox table is named uniquely (e.g., `aik_externalai_outbox_messages` or distinct from AiOrchestration's outbox)
2. **Verify outbox table uniqueness:**
   - Check that ExternalAiDbContext and AiOrchestrationDbContext do NOT share the same outbox table name
   - If they share the same database, each must have a distinct outbox table (e.g., `aik_orchestration_outbox_messages` and `aik_externalai_outbox_messages`)
   - Check the `OutboxTableName` override in both DbContexts
3. **Verify entity type configurations** use correct `ToTable("aik_...")` calls
4. **Check model snapshot consistency:**
   - `dotnet ef migrations has-pending-model-changes --context ExternalAiDbContext --project src/modules/aiknowledge/NexTraceOne.AIKnowledge.Infrastructure/`
5. **Test migration application** against test database
6. **Verify `idempotency_key` unique index** on outbox table

## 9. Responsabilidades permitidas
- Verify existing migration content
- Add corrective migration if prefix issues found
- Fix `OutboxTableName` override if needed (for uniqueness)
- Run migration commands

## 10. Responsabilidades proibidas
- Do NOT delete existing migration files
- Do NOT modify domain entities
- Do NOT modify NexTraceDbContextBase
- Do NOT modify AiOrchestrationDbContext migrations
- Do NOT modify the frontend

## 11. Critérios de aceite
- [ ] All tables in ExternalAiDbContext use `aik_` prefix
- [ ] Outbox table has a unique name (no conflict with AiOrchestrationDbContext)
- [ ] `idempotency_key` unique index exists on outbox table
- [ ] Model snapshot is consistent
- [ ] Migration applies cleanly
- [ ] Entity type configurations match migration output
- [ ] Build succeeds with zero new errors

## 12. Validações obrigatórias
- `dotnet build` succeeds for AIKnowledge.Infrastructure project
- `dotnet ef migrations has-pending-model-changes` reports no pending changes
- Table prefix convention matches `docs/architecture/database-table-prefixes.md`
- Outbox table name is unique across all DbContexts in the same database
- No changes to other module files

## 13. Riscos e cuidados
- **LOW RISK:** Verification task — migration already exists
- **CAUTION:** ExternalAiDbContext and AiOrchestrationDbContext may share the `nextraceone_ai` database — outbox table names MUST be distinct to avoid processing conflicts
- **CAUTION:** `ExternalAiPolicy` may contain sensitive configuration — verify encrypted fields are properly configured in entity type configuration
- **CAUTION:** `KnowledgeCapture` entity may overlap with Knowledge module — verify this is intentional and doesn't create a source-of-truth conflict

## 14. Dependências
- **Depends on:** Nothing
- **Unblocks:** P02.7 (IExternalAiModule implementation needs verified migration)
- **Can run in parallel with:** P02.8a, P02.8b, P02.8c, P02.8e, P02.8f, P02.8g (group-C)

## 15. Entregáveis esperados
- Verified migration file with correct `aik_` table prefixes
- Verified outbox table uniqueness
- Corrective migration (if issues found)
- Verification report: table names, prefix compliance, outbox uniqueness, model snapshot consistency
