# PROMPT — Verify and remove InMemoryIncidentStore from the OperationalIntelligence module

## 1. Identification
- **Prompt ID:** P06.4
- **Phase:** 06-cleanup-consolidation
- **Execution order:** 48
- **Type:** cleanup
- **Area:** operational-intelligence
- **Module:** operationalintelligence
- **Bounded context:** incident-store
- **Can run in parallel:** yes (group-F, after P01.2 completes)
- **Risk level:** low

## 2. Objective
Verify that `InMemoryIncidentStore` is not actively used in production DI registration or any live code path, then safely remove it from the codebase. The real implementation `EfIncidentStore` (678 lines, backed by `IncidentDbContext` with confirmed EF Core migrations) is the canonical persistence layer for incidents and must remain untouched.

## 3. Problem actual
Per forensic audit: "InMemoryIncidentStore may still exist as residual reference. EfIncidentStore (678 lines) is the real implementation." The audit confirmed:

- `EfIncidentStore` exists at `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/EfIncidentStore.cs` — 678 lines of real EF Core persistence
- `IncidentDbContext` has confirmed migrations and real schema
- `InMemoryIncidentStore` exists at `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/InMemoryIncidentStore.cs` — likely a development stub or early prototype
- `IncidentSeedData.cs` exists in the same directory and may reference InMemoryIncidentStore

The InMemoryIncidentStore is dead code if not registered in DI. If it is registered in DI (perhaps as a fallback or development mode), it must be replaced with the real EfIncidentStore registration. Either way, the in-memory implementation should be removed to prevent future confusion about which store is canonical.

## 4. Resultado esperado
- `InMemoryIncidentStore.cs` deleted from the repository (if confirmed not in active DI)
- If `InMemoryIncidentStore` was registered in DI, the registration is replaced with `EfIncidentStore`
- All test references updated to use `EfIncidentStore` or mock `IIncidentStore` interface
- `IncidentSeedData.cs` updated if it depends on `InMemoryIncidentStore`
- Build succeeds
- All OperationalIntelligence tests pass
- Zero references to `InMemoryIncidentStore` remain in the codebase

## 5. Escopo permitido
- **Modules:** `src/modules/operationalintelligence/` only
- **Directories:**
  - `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/`
  - `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/` (DI registration)
  - `tests/modules/operationalintelligence/` (test updates if needed)
- **Operations:** file deletion, DI registration update, test reference update
- **Artifact types:** C# source files, C# test files

## 6. Escopo proibido
- Do NOT modify `EfIncidentStore.cs` — the real implementation must remain untouched
- Do NOT modify `IncidentDbContext` or any EF Core migrations
- Do NOT modify incident domain entities (`src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Domain/`)
- Do NOT modify other modules (`src/modules/governance/`, `src/modules/catalog/`, etc.)
- Do NOT modify frontend code (`src/frontend/`)
- Do NOT modify API layer (`src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.API/`)
- Do NOT create new database migrations
- Do NOT modify the `IIncidentStore` interface (or equivalent abstraction)

## 7. Ficheiros principais candidatos a alteração
1. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/InMemoryIncidentStore.cs` (DELETE)
2. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/IncidentSeedData.cs` (update if references InMemoryIncidentStore)
3. `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/DependencyInjection.cs` (or equivalent DI registration file — update if InMemoryIncidentStore is registered)
4. `tests/modules/operationalintelligence/` — any test files referencing InMemoryIncidentStore

## 8. Passos obrigatórios
1. **Locate InMemoryIncidentStore** — Confirm the file exists:
   ```
   find src/modules/operationalintelligence/ -name "InMemoryIncidentStore*"
   ```
2. **Check DI registration** — Search for InMemoryIncidentStore in DI setup:
   ```
   grep -rn "InMemoryIncidentStore" src/modules/operationalintelligence/ --include="*.cs"
   ```
   Look for `AddScoped<IIncidentStore, InMemoryIncidentStore>`, `AddSingleton`, `AddTransient`, or similar patterns.
3. **Check test references** — Search test projects:
   ```
   grep -rn "InMemoryIncidentStore" tests/ --include="*.cs"
   ```
4. **Check seed data dependency** — Examine `IncidentSeedData.cs`:
   ```
   grep -n "InMemoryIncidentStore" src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/IncidentSeedData.cs
   ```
5. **Decision gate:**
   - **If NOT in active DI:** Proceed to delete the file directly.
   - **If in active DI:** Replace registration with `EfIncidentStore` (P01.2 should have already done this — verify, then update registration).
   - **If referenced in tests:** Update tests to either use `EfIncidentStore` directly or mock `IIncidentStore` interface.
   - **If referenced in IncidentSeedData.cs:** Refactor seed data to not depend on InMemoryIncidentStore.
6. **Delete InMemoryIncidentStore.cs** — Remove the file using `git rm`.
7. **Update any remaining references** — Fix any compilation errors caused by the deletion (DI registration, seed data, tests).
8. **Build verification** — Run:
   ```
   dotnet build src/modules/operationalintelligence/
   ```
9. **Test verification** — Run:
   ```
   dotnet test tests/modules/operationalintelligence/
   ```
10. **Final scan** — Confirm zero remaining references:
    ```
    grep -rn "InMemoryIncidentStore" . --include="*.cs"
    ```

## 9. Responsabilidades permitidas
- Delete `InMemoryIncidentStore.cs` from the codebase
- Update DI registration to use `EfIncidentStore` if InMemoryIncidentStore was registered
- Update `IncidentSeedData.cs` to remove InMemoryIncidentStore dependency
- Update test files to remove InMemoryIncidentStore references
- Fix compilation errors caused by the deletion

## 10. Responsabilidades proibidas
- Must NOT modify `EfIncidentStore.cs`
- Must NOT modify `IncidentDbContext` or EF Core migrations
- Must NOT modify incident domain entities or value objects
- Must NOT modify the `IIncidentStore` interface contract
- Must NOT modify other modules' code
- Must NOT modify frontend code
- Must NOT modify API endpoints
- Must NOT introduce new persistence implementations
- Must NOT create new database migrations

## 11. Critérios de aceite
- [ ] `InMemoryIncidentStore.cs` no longer exists in the repository
- [ ] `grep -rn "InMemoryIncidentStore" src/ --include="*.cs"` returns zero results
- [ ] `grep -rn "InMemoryIncidentStore" tests/ --include="*.cs"` returns zero results
- [ ] DI registration for `IIncidentStore` (or equivalent) points to `EfIncidentStore`
- [ ] `dotnet build src/modules/operationalintelligence/` succeeds without errors
- [ ] All OperationalIntelligence tests pass without regressions
- [ ] `EfIncidentStore.cs` is unmodified (verified via `git diff`)
- [ ] No domain entities or migrations modified

## 12. Validações obrigatórias
- `dotnet build src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/` — must succeed
- `dotnet build NexTraceOne.sln` — must succeed (no cross-module breakage)
- `dotnet test tests/modules/operationalintelligence/` — all tests must pass (if test project exists)
- `grep -rn "InMemoryIncidentStore" . --include="*.cs" --exclude-dir=.git` — must return zero results
- `git diff src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/EfIncidentStore.cs` — must show no changes

## 13. Riscos e cuidados
- **LOW RISK** — removing a dead-code in-memory stub that has been superseded by a real EF Core implementation
- **DI registration risk:** If InMemoryIncidentStore is registered as a fallback for development environments (e.g., behind a feature flag or environment check), removing it without updating the registration will cause runtime DI resolution failure. Always check the registration first.
- **Seed data coupling:** `IncidentSeedData.cs` may instantiate InMemoryIncidentStore directly for seeding. If so, refactor to use the `IIncidentStore` interface or seed via `IncidentDbContext` directly.
- **Test dependency:** Some integration or unit tests may use InMemoryIncidentStore as a lightweight test double. These should be updated to mock `IIncidentStore` instead, which is a better testing practice.
- **P01.2 prerequisite:** If P01.2 (real incidents persistence) has not yet completed, InMemoryIncidentStore may still be the active store. In that case, this prompt should wait until P01.2 confirms EfIncidentStore is the active DI registration.

## 14. Dependências
- **Depends on:** P01.2 (real incidents persistence — EfIncidentStore must be confirmed as the active DI-registered implementation before InMemoryIncidentStore can be safely removed)
- **Unblocks:** Clean codebase; no future confusion about which incident store is canonical; clearer module architecture
- **Blocked by:** P01.2 must confirm EfIncidentStore is registered and functional

## 15. Entregáveis esperados
- Deleted file: `src/modules/operationalintelligence/NexTraceOne.OperationalIntelligence.Infrastructure/Incidents/InMemoryIncidentStore.cs`
- Updated DI registration (if InMemoryIncidentStore was registered)
- Updated `IncidentSeedData.cs` (if it referenced InMemoryIncidentStore)
- Updated test files (if any referenced InMemoryIncidentStore)
- Build and test verification output
- Report: DI registration state before/after, test impact, files modified
