# PROMPT — Remove all remaining references to the removed Commercial Governance module

## 1. Identification
- **Prompt ID:** P06.3
- **Phase:** 06-cleanup-consolidation
- **Execution order:** 47
- **Type:** cleanup
- **Area:** governance
- **Module:** cross-cutting
- **Bounded context:** commercial-gov
- **Can run in parallel:** yes (group-F)
- **Risk level:** low

## 2. Objective
Remove all remaining references to the Commercial Governance module — which was removed in PR-17 — from the entire repository. This includes source code comments, documentation mentions, configuration entries, test references, and any other artifacts that still treat Commercial Governance as an active or planned module. After this prompt, no contributor should encounter a reference that implies Commercial Governance exists.

## 3. Problem actual
Per forensic audit: "Commercial Governance removed in PR-17 but references may remain." The module was deliberately removed as part of a scope decision, but vestiges likely persist across the codebase:

- Documentation files may still list it as a module or planned capability
- Source code comments may reference it as a dependency, integration point, or future feature
- Configuration files may contain entries for it
- Execution prompts inventory and mapping files reference it
- Navigation or routing configuration might include commented-out entries
- Test files might reference types or namespaces from the removed module

These residual references create confusion about the product's actual scope and module inventory, undermining the NexTraceOne Source of Truth principle.

Evidence from prior search:
- `docs/execution-prompts/PROMPT-INVENTORY.csv` — references Commercial Governance
- `docs/execution-prompts/PROBLEM-TO-PROMPT-MAPPING.csv` — maps Commercial Governance problems
- `docs/execution-prompts/EXECUTION-PROMPTS-SEQUENCE.md` — references in governance context
- `docs/execution-prompts/GATES-AND-DEPENDENCIES.md` — governance dependencies mention
- `docs/audit-forensic-2026-03/` — documents the removal (do NOT modify these)
- `docs/archive/old-phase-reports/` — historical references (do NOT modify these)

## 4. Resultado esperado
- Zero active references to "Commercial Governance" as an existing or planned module anywhere in the repository (excluding archive and audit-forensic docs which are historical records)
- Source code contains no imports, usings, comments, or stubs referencing CommercialGovernance
- Documentation either removes the reference or annotates it with "(Removed in PR-17)"
- Configuration files contain no entries for the removed module
- Build succeeds with no regressions
- All tests pass with no regressions

## 5. Escopo permitido
- **Directories:** Entire repository (scan all), but modifications limited to:
  - `src/` — remove code references (comments, imports, stubs)
  - `docs/` — remove or annotate documentation references (except audit-forensic and archive)
  - `docs/execution-prompts/` — update inventory and mapping files
  - Root configuration files if they contain module registration entries
- **Operations:** text removal, annotation addition, comment cleanup
- **Artifact types:** C# files, markdown files, CSV files, YAML/JSON config files

## 6. Escopo proibido
- Do NOT modify `docs/audit-forensic-2026-03/` — these are forensic records of the removal decision
- Do NOT modify `docs/archive/` — these are historical records and should preserve original content
- Do NOT modify active module logic — only remove dead references to the non-existent module
- Do NOT remove any active functionality that might share a name but belongs to the existing `governance` module
- Do NOT modify database migrations (even if they reference CommercialGovernance, migrations are immutable history)
- Do NOT modify git history or commit messages

## 7. Ficheiros principais candidatos a alteração
1. `docs/execution-prompts/PROMPT-INVENTORY.csv` — remove/annotate Commercial Governance entries
2. `docs/execution-prompts/PROBLEM-TO-PROMPT-MAPPING.csv` — remove/annotate Commercial Governance mappings
3. `docs/execution-prompts/EXECUTION-PROMPTS-SEQUENCE.md` — remove/annotate references
4. `docs/execution-prompts/GATES-AND-DEPENDENCIES.md` — remove/annotate dependency references
5. Any `.cs` files under `src/` containing `CommercialGovernance` in comments or using statements
6. Any documentation files under `docs/` containing active Commercial Governance references

## 8. Passos obrigatórios
1. **Full repository scan** — Search the entire repository for all variants:
   - `grep -rn "Commercial Governance" . --include="*.md" --include="*.cs" --include="*.csv" --include="*.json" --include="*.yaml" --include="*.yml" --include="*.csproj" --include="*.tsx" --include="*.ts"`
   - `grep -rn "CommercialGovernance" . --include="*.cs" --include="*.csproj" --include="*.json"`
   - `grep -rn "commercial-governance" . --include="*.md" --include="*.yaml" --include="*.yml" --include="*.json"`
   - `grep -rn "commercial_governance" . --include="*.cs" --include="*.json" --include="*.yaml"`
2. **Classify each reference** — For each match, classify as:
   - **ARCHIVE/AUDIT** — in `docs/audit-forensic-2026-03/` or `docs/archive/` → SKIP (do not modify)
   - **SOURCE CODE** — in `src/` → remove the reference (import, using, comment, stub, etc.)
   - **DOCUMENTATION** — in `docs/` (active) → add "(Removed in PR-17)" annotation or remove the line if it adds no value
   - **CONFIGURATION** — in config files → remove the entry
   - **TEST CODE** — in `tests/` → remove if test is for removed functionality; update comment if just a reference
   - **EXECUTION PROMPTS** — in `docs/execution-prompts/` → annotate as "(Removed in PR-17)" in CSV/MD files
3. **Apply changes** — For each classified reference:
   - Source code: remove the line or block. If removing a line leaves an orphaned comment block, clean up the surrounding context.
   - Documentation: add annotation or remove the reference line. Preserve surrounding structure.
   - Configuration: remove the entry. Ensure JSON/YAML remains valid after removal.
   - CSV files: add annotation in the relevant column or remove the row if entirely about Commercial Governance.
4. **Verify build** — Run `dotnet build` on the full solution to confirm no compilation regressions.
5. **Verify tests** — Run `dotnet test` on any affected test projects to confirm no test regressions.
6. **Final verification scan** — Re-run the grep commands from step 1 to confirm the only remaining references are in archive/audit-forensic directories.
7. **Document results** — List every file modified, the nature of the change, and the reference that was removed/annotated.

## 9. Responsabilidades permitidas
- Remove dead code references (comments, imports, stubs) to CommercialGovernance in source files
- Add "(Removed in PR-17)" annotations to active documentation references
- Remove configuration entries for the non-existent module
- Update execution prompt inventory and mapping files
- Clean up orphaned code blocks left after reference removal

## 10. Responsabilidades proibidas
- Must NOT modify forensic audit reports (`docs/audit-forensic-2026-03/`)
- Must NOT modify archived documents (`docs/archive/`)
- Must NOT modify database migrations
- Must NOT remove or alter any functionality belonging to the active `governance` module
- Must NOT modify git history
- Must NOT introduce new code or features — this is cleanup only
- Must NOT modify frontend routing or navigation unless it explicitly contains CommercialGovernance references

## 11. Critérios de aceite
- [ ] `grep -rn "CommercialGovernance" src/` returns zero results
- [ ] `grep -rn "Commercial Governance" docs/ --exclude-dir=audit-forensic-2026-03 --exclude-dir=archive` returns zero active (non-annotated) references
- [ ] `grep -rn "commercial-governance" . --exclude-dir=docs/audit-forensic-2026-03 --exclude-dir=docs/archive --exclude-dir=.git` returns zero results
- [ ] `dotnet build NexTraceOne.sln` succeeds without errors
- [ ] All existing tests pass without regressions
- [ ] No active documentation treats Commercial Governance as an existing or planned module
- [ ] Execution prompt inventory and mapping files updated/annotated

## 12. Validações obrigatórias
- `dotnet build NexTraceOne.sln` — must succeed
- `dotnet test` on affected test projects — must pass
- Final grep scan confirming only archive/audit references remain
- `git diff --stat` shows no deletions of entire feature directories (only line-level removals and annotations)
- Modified CSV files remain valid CSV format
- Modified JSON/YAML files remain valid syntax

## 13. Riscos e cuidados
- **LOW RISK** — removing references to an already-deleted module
- **False positive risk:** The word "commercial" or "governance" may appear in contexts unrelated to the removed module. Always verify the full context before removing — only remove references specifically to "Commercial Governance" as a module/feature.
- **Active governance module:** The `governance` module under `src/modules/governance/` is active and must not be touched. Only references to the distinct "Commercial Governance" module (removed in PR-17) should be cleaned.
- **CSV format integrity:** When editing CSV files, ensure column alignment and quoting remain valid.
- **Execution prompt references:** Some execution prompts may reference Commercial Governance as context for why certain governance work exists. Annotate rather than remove to preserve execution context.

## 14. Dependências
- **Depends on:** Nothing — this cleanup can proceed independently
- **Unblocks:** Codebase hygiene; clearer module inventory; reduced confusion for new contributors
- **Blocked by:** None

## 15. Entregáveis esperados
- Modified source files with CommercialGovernance references removed (list each file)
- Modified documentation files with references annotated or removed (list each file)
- Modified configuration/CSV files with entries cleaned (list each file)
- Final grep verification report confirming only archive/audit references remain
- Summary: total references found, classified, and resolved per category
