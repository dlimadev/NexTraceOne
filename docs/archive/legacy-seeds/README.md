# Archive: Legacy Seeds

> Arquivado em: 2026-03-27 (P12.3)

Scripts SQL de seed criados antes da normalização de prefixos de tabela.
Estes ficheiros **não podem ser executados directamente** — usam prefixos de tabela incorrectos (`identity_*`, `eg_*`, `ct_*`, `oi_*`).

**Não usar para seed de base de dados.**  
Para seeds actuais, ver `src/platform/NexTraceOne.ApiHost/DataSeed/` ou scripts de migração EF Core.
